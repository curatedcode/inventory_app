extends ../layout

block content 
  h1= title

  p.delete Do you really want to delete this Category?

  form(method='POST' action='')
    div
      input#item(type='hidden',name='category', required='true', value=item.name )

    button.submit(type='submit') Delete